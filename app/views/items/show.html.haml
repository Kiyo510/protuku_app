%h2 出品詳細ページ

-#stockボタン
%div{:id => "item-#{@item.id}"}
  = render partial: 'stocks/stock_btn', locals: { item: @item }

  -# twitterシェア
= link_to "https://twitter.com/intent/tweet?url=#{request.url}/&hashtags=宿題代行", title: 'Twitter', target: 'blank' do
  = fa_icon 'twitter'

= link_to 'カードで購入する', purchase_items_path(@item)
%p
  = link_to '投稿の編集', edit_item_path(@item)
%p
  = link_to '投稿の削除', item_path(@item), method: :delete, data: { confirm: "投稿内容はデータから完全に削除されます。本当に削除しますか？" }
